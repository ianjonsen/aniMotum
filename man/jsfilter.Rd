% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jsfilter.R
\name{jsfilter}
\alias{jsfilter}
\title{Fit a joint (hierarchical) move persistence state-space model}
\usage{
jsfilter(
  x,
  time.step = NA,
  share = share_control(),
  parameters = NULL,
  map = NULL,
  fit.to.subset = TRUE,
  control = ssm_control(),
  inner.control = NULL,
  ho_lookup = NULL,
  group_lookup = NULL,
  init = c("individual", "moment")
)
}
\arguments{
\item{x}{a named list of prefiltered sf-tibbles, one per individual, as
produced by \link{prefilter}}

\item{time.step}{prediction interval in hours, a data.frame of prediction
times, or NA}

\item{share}{a parameter sharing specification from
\link{share_control}}

\item{parameters}{optional named list of initial values}

\item{map}{optional named list of parameters to fix during estimation}

\item{fit.to.subset}{fit to the prefiltered subset of observations}

\item{control}{a list of control values from \link{ssm_control}}

\item{inner.control}{control settings for the inner optimiser}

\item{ho_lookup}{optional haulout lookup assembled by \link{fit_ssm}}

\item{group_lookup}{optional data.frame with \code{id} and a grouping variable}

\item{init}{one of \code{"individual"} (default) to initialise from separate
per-individual \code{mp} fits, or \code{"moment"} for moment-based starting values.
Per-individual initialisation costs an extra pass over the data but
substantially improves convergence, because it supplies starting states
that are consistent with the starting parameters}
}
\value{
a named list of \code{mp_ssm} objects, one per individual, all sharing
the single joint fit's optimiser output, TMB object and sdreport
}
\description{
Fits an \code{mp} state-space model to several individual tracks at
once, sharing parameters among individuals according to a
\link{share_control} specification. Called by \link{fit_ssm} when
\code{model = "jmp"}.
}
\details{
The model is written in R using RTMB rather than as a C++
template. A joint fit builds one AD object for the whole data set, so
RTMB's taping cost is paid once rather than once per individual.

By default the measurement model and \code{sigma_g} are pooled, the process
innovation scale is hierarchical, and \code{rho_p} is estimated separately per
individual. See \link{share_control} for the reasoning behind each of
those choices, and for the combinations that are refused.
}
\keyword{internal}
