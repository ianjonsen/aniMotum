% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jmp_nll.R
\name{jmp_nll}
\alias{jmp_nll}
\title{Joint (hierarchical) move persistence state-space model
negative log-likelihood}
\usage{
jmp_nll(dat)
}
\arguments{
\item{dat}{a named list of model data assembled by \link{jsfilter}}
}
\value{
a function of the parameter list, suitable for \code{RTMB::MakeADFun}
}
\description{
Returns the RTMB objective function for a time-varying move
persistence model fitted jointly to multiple individual tracks, with
parameters shared among individuals according to a
\link{share_control} specification.
}
\details{
This is the R-language counterpart of \code{src/TMB/sub/mp.hpp},
extended across individuals. The process and observation models are
identical to the single-individual template, including the \code{gap_flag} and
\code{ho_flag} handling.

The parameter that matters most here is \code{sigma_g}, the scale of the random
walk on \code{logit(gamma)}. It is pooled across individuals by default, for the
reason set out in Jonsen et al. (2019) and already embodied in
\code{joint_mpm.hpp}: with an individual-specific \code{sigma_g}, each animal's
\code{g_t} series is smoothed on its own scale and the series are not comparable
between animals. Comparability is usually the entire reason for fitting a
move persistence model to a deployment.

As in \link{jcrw_nll} (now superseded), the process covariance is a
scalar multiple of a matrix that is constant within an individual, so it is
inverted analytically once per individual rather than factorised at every
time step: for a scalar \code{s}, \verb{det(sC) = s^2 det(C)} and \code{(sC)^-1 = C^-1 / s}.
The observation likelihood is evaluated in three vectorised blocks.
}
\references{
Jonsen ID, McMahon CR, Patterson TA, et al. (2019) Movement
responses to environment: fast inference of variation among southern
elephant seals with a mixed effects model. Ecology 100(1):e02566
}
\keyword{internal}
