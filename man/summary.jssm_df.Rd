% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.jssm_df.R
\name{summary.jssm_df}
\alias{summary.jssm_df}
\title{summarise a joint (hierarchical) ssm fit}
\usage{
\method{summary}{jssm_df}(object, ...)
}
\arguments{
\item{object}{a joint ssm fit object with class \code{jssm_df}}

\item{...}{unused. For compatibility with the generic method.}
}
\value{
a list with components \code{Fittab} (the single fit), \code{Stattab} (one
row per individual), \code{Shared} (parameters common to all individuals) and
\code{Partab} (parameters that vary, one table per individual)
}
\description{
A joint fit is one model fitted to all tracks at once, not one
model per track. The returned \code{ssm_df} keeps a row per individual because
the estimated states really are per individual, but the objective,
convergence and AICc describe the whole fit. This method reports them once,
rather than repeating them against every animal as the \code{ssm_df} method
would, and separates the parameters shared by all individuals from those
that vary.
}
