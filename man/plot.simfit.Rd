% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.simfit.R
\name{plot.simfit}
\alias{plot.simfit}
\title{plot}
\usage{
\method{plot}{simfit}(
  x,
  type = c("lines", "points", "both"),
  zoom = TRUE,
  or = NULL,
  ncol = 1,
  hires = FALSE,
  ...
)
}
\arguments{
\item{x}{a \code{foieGras} simulation data.frame with class \code{simfit}}

\item{type}{plots tracks as "line", "points" or "both" (default).}

\item{zoom}{logical; should map extent be defined by track extent (TRUE; default) or
should global map be drawn (FALSE).}

\item{or}{orientation of projected map, default is to centre on
start of fitted track (ignored if \code{mapproj} package is not installed).}

\item{ncol}{number of columns to arrange multiple plots}

\item{hires}{logical; use high-resolution coastline data. Attempts to use
high-res coastline data via \link[rnaturalearth:ne_countries]{rnaturalearth::ne_countries} with \code{scale = 10},
if the \code{rnaturalearthhires} data package is installed. This extends the
plot rendering time so is set to FALSE by default, in which case
\link[rnaturalearth:ne_countries]{rnaturalearth::ne_countries} with \code{scale = 50} data are used.}

\item{...}{additional arguments to be ignored}
}
\value{
Plots of simulated tracks.
}
\description{
visualize tracks simulated from a \code{foieGras} model fit
}
\examples{
fit <- fit_ssm(ellie, model = "crw", time.step = 24)
trs <- simfit(fit, what = "p", reps = 2)
plot(trs, type = "b")

}
