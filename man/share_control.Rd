% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/share_control.R
\name{share_control}
\alias{share_control}
\title{Specify which parameters are shared among individuals}
\usage{
share_control(
  sigma = c("hierarchical", "pooled", "individual"),
  sigma_g = c("pooled", "individual"),
  rho_p = c("individual", "pooled"),
  tau = c("pooled", "individual", "hierarchical"),
  psi = c("pooled", "individual", "hierarchical"),
  rho_o = c("pooled", "individual"),
  hier.sigma = c("shared", "xy"),
  ho_scale = c("fixed", "pooled"),
  group = NULL,
  strict = TRUE
)
}
\arguments{
\item{sigma}{treatment of the process innovation scale. One of
\code{"hierarchical"} (default), \code{"pooled"} or \code{"individual"}}

\item{sigma_g}{treatment of the move persistence random walk scale. One of
\code{"pooled"} (default) or \code{"individual"}. Pooling is what makes \code{g_t}
comparable among individuals}

\item{rho_p}{treatment of the process error correlation between x and y.
One of \code{"individual"} (default) or \code{"pooled"}}

\item{tau}{treatment of the LS/GPS measurement error dispersion. One of
\code{"pooled"} (default), \code{"individual"} or \code{"hierarchical"}}

\item{psi}{treatment of the Argos error ellipse semi-minor axis scaling.
One of \code{"pooled"} (default), \code{"individual"} or \code{"hierarchical"}}

\item{rho_o}{treatment of the measurement error correlation between x and
y. One of \code{"pooled"} (default) or \code{"individual"}}

\item{hier.sigma}{structure of the individual random effect on \code{sigma} when
\code{sigma = "hierarchical"}. \code{"shared"} (default) gives one scalar deviation
per individual applied to both coordinates; \code{"xy"} gives independent
deviations per coordinate. \code{"shared"} is the default because it is the
rotation-invariant choice}

\item{ho_scale}{treatment of the haulout process variance scale factor.
\code{"fixed"} (default) uses the constant from \link{ssm_control};
\code{"pooled"} estimates it. Estimating it is only possible in a joint fit: it
is not identifiable from one track}

\item{group}{name of a variable in the input data defining strata within
which the measurement parameters are pooled, or NULL (default)}

\item{strict}{logical; if TRUE (default), reject parameter combinations
that are not identifiable. If FALSE, warn instead}
}
\value{
a named list of parameter sharing settings
}
\description{
\code{share_control} selects how each parameter of a joint
(hierarchical) state-space model is treated across individual tracks, and
guards against parameter-sharing combinations that are not identifiable.

Each parameter may be:
\describe{
\item{\code{"pooled"}}{a single value estimated from all individuals at once.}
\item{\code{"hierarchical"}}{an individual-level value drawn from an estimated
population distribution, so individual estimates are partially pooled
toward the population mean.}
\item{\code{"individual"}}{a separate, independent value per individual, as in
\link{fit_ssm}.}
}
}
\details{
The defaults implement the recommended structure for the joint
move persistence model: a pooled measurement model, a pooled \code{sigma_g}, a
hierarchical process innovation scale, and an individual \code{rho_p}.

\strong{Measurement model.} \code{tau}, \code{psi} and \code{rho_o} describe the
observation system rather than the animal. Argos error is a property of
satellite geometry, transmitter power, antenna and surfacing behaviour, and
is shared by animals carrying comparable tags, so estimating these
separately per individual estimates one quantity many times using a
fraction of the data each time. \code{psi} is the clearest case: it scales only
the semi-minor axis of the Argos error ellipse, so it is informed solely by
the anisotropy of the observation residuals and is weakly identified from a
single track. Note that \code{tau} scales fixed per-\code{lc} variances taken from
independent data (see \link{emf}) rather than estimating the error
from scratch, so a pooled \code{tau} far from 1 is a statement about how well
those independent values are calibrated for the deployment at hand.

\strong{\code{sigma_g} is pooled, and this is the point of the model.} With an
individual-specific \code{sigma_g}, each animal's \code{g_t} series is smoothed on
its own scale and the series cannot be compared between animals.
Comparability is usually the entire reason for fitting a move persistence
model to a deployment. This follows Jonsen et al. (2019) and the existing
\code{jmpm} model in \link{fit_mpm}. \code{sigma_g} is also rotation-invariant:
it scales a random walk on a scalar, so it does not depend on which way the
animal travelled.

\strong{\code{rho_p} is individual by default, and this is deliberate.} \code{rho_p}
is the correlation between the x and y process innovations, which sets the
tilt of the process covariance ellipse relative to the projected
coordinate axes. It therefore records the animal's direction of travel
rather than a property of its movement. Animals heading in different
directions are not exchangeable in \code{rho_p}, and pooling it averages
correlations that may differ in sign, producing an estimate near zero that
reflects nothing but the mixture. Pool it only when all individuals travel
broadly the same way.

\strong{The residual frame-dependence.} \code{hier.sigma = "shared"} gives each
individual one scalar deviation applied to both coordinates, so an
individual's process covariance is a scalar multiple of the population one.
A scalar multiple is rotation-invariant, so the estimated among-individual
variance is a statement about movement magnitude rather than heading. What
it does hold common is the \code{sigma_x} : \code{sigma_y} ratio, which is
frame-dependent. That is a known approximation, and a fully
rotation-invariant parameterisation - overall magnitude, anisotropy ratio,
and orientation, with orientation always individual - is the intended
replacement.

\strong{Identifiability.} The central tension in a state-space model is the
partitioning of total variance between the process and the measurement
model. Random effects on both sides let the individual-level deviations
trade off against each other at almost no cost in likelihood, leaving the
variance hyperparameters weakly identified and, with short tracks,
effectively unidentified. \code{share_control} therefore refuses to make \code{sigma}
hierarchical at the same time as \code{tau} or \code{psi}. Set \code{strict = FALSE} to
downgrade this to a warning.

\strong{Grouping.} Pooling the measurement parameters assumes a homogeneous
observation process. Mixed tag models, different duty cycles, or different
species all violate that. Use \code{group} to name a variable in the input data
defining strata within which the measurement parameters are pooled.
}
\examples{
## the recommended defaults
share_control()

## pool the measurement model within tag type
share_control(group = "tag_type")

## fully pooled process scale, for too few individuals to estimate
## among-individual variance
share_control(sigma = "pooled")

}
\references{
Jonsen ID, McMahon CR, Patterson TA, et al. (2019) Movement
responses to environment: fast inference of variation among southern
elephant seals with a mixed effects model. Ecology 100(1):e02566
}
