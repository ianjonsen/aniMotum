% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ssm_prep.R
\name{ssm_prep}
\alias{ssm_prep}
\title{Prepare a single track for state-space model fitting}
\usage{
ssm_prep(
  x,
  time.step = NA,
  fit.to.subset = TRUE,
  control = ssm_control(),
  ho_lookup = NULL
)
}
\arguments{
\item{x}{an sf-tibble for a single individual, as produced by
\link{prefilter}}

\item{time.step}{prediction interval in hours, a data.frame of prediction
times with \code{id} and \code{date}, or NA to estimate states at observation times
only}

\item{fit.to.subset}{fit to the prefiltered subset of observations (TRUE)
or to all observations (FALSE)}

\item{control}{a list of control values from \link{ssm_control}}

\item{ho_lookup}{optional data.frame of haulout indicators keyed on \code{id}
and \code{date}, as assembled by \link{fit_ssm}}
}
\value{
a named list with components \code{d.all} (the merged observation and
prediction grid), \code{prj} (the coordinate reference system), \code{dt}, \code{Y},
\code{isd}, \code{obs_mod}, \code{K}, \code{m}, \code{M}, \code{c}, \code{GLerr}, \code{state0}, \code{gap_flag},
\code{ho_flag}, \code{xs} (state initial values), \code{obs.types} (observation types
present) and \code{x} (the input data)
}
\description{
Builds the prediction grid, time differences, haulout and data
gap indicators, observation model indices, and state initial values for a
single individual. This is the data preparation stage required by all of
aniMotum's state-space models.

At present \code{ssm_prep} is used only by the joint (hierarchical) filter,
\link{jsfilter}. \code{sfilter} and \code{mpfilter} each carry their own copy of
this logic (they share 358 identical lines between them); migrating both
onto \code{ssm_prep} is the intended follow-up once this function has been
validated against their existing behaviour. It is written to be a drop-in
replacement for that block rather than a parallel implementation.
}
\keyword{internal}
