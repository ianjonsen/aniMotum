% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jcrw_nll.R
\name{jcrw_nll}
\alias{jcrw_nll}
\title{Joint (hierarchical) correlated random walk with a rotation-invariant
process covariance}
\usage{
jcrw_nll(dat)
}
\arguments{
\item{dat}{a named list of model data assembled by \link{jsfilter}}
}
\value{
a function of the parameter list, suitable for \code{RTMB::MakeADFun}
}
\description{
Returns the RTMB objective function for a continuous-time
correlated random walk fitted jointly to multiple individual tracks, with
parameters shared among individuals according to a
\link{share_control} specification.
}
\details{
This is the R-language counterpart of \code{src/TMB/sub/crw.hpp},
extended across individuals and reparameterised so that the quantities
shared among individuals do not depend on which way each animal travelled.

\strong{Why the reparameterisation.} \code{crw.hpp} describes the velocity
innovation covariance with \code{D_x}, \code{D_y} and \code{rho_p}, which are the axes and
tilt of an ellipse measured against the projected coordinate axes. All
three change when an animal travels a different bearing, even if its
movement is otherwise identical. Pooling them across a deployment therefore
averages quantities that mean different things for different animals, and
an among-individual variance estimated from them is partly a statement
about heading.

\strong{The parameterisation.} The same covariance is written

\if{html}{\out{<div class="sourceCode">}}\preformatted{  V = exp(m) * expm(A)        A = [ u1   u2 ]
                                  [ u2  -u1 ]
}\if{html}{\out{</div>}}

\code{A} is symmetric with zero trace, so \code{exp(A)} has determinant one and
\verb{det(V) = exp(2m)} exactly. \code{A} has eigenvalues \verb{+/- r}, where \code{r} is
\code{sqrt(u1^2 + u2^2)}, so \code{V} has eigenvalues \verb{exp(m +/- r)} and its
major axis lies at \code{theta = atan2(u2, u1) / 2}. That gives

\describe{
\item{\code{m}}{overall movement magnitude, half the log determinant. Rotation
invariant: rotate the coordinate frame and it does not move.}
\item{\code{r}}{log anisotropy, half the log ratio of the ellipse axes. Also
rotation invariant.}
\item{\code{theta}}{orientation. This is the only frame-dependent piece, and
it is never pooled.}
}

The \code{2} in \code{atan2(u2, u1) / 2} is what makes \code{u} well behaved: an ellipse is
unchanged by a half turn, so orientation lives on a half circle, and using
\verb{2 theta} maps that onto a full circle. \code{u} is then an ordinary unconstrained
vector with no wrapping to handle.

Because \code{A} is traceless and symmetric, \verb{exp(A) = cosh(r) I + sinh(r)/r A},
so neither an eigendecomposition nor a matrix inverse is needed - the log
determinant is \verb{2m} and the quadratic form is written out directly. That
keeps the AD tape small, which matters more in RTMB than in C++.
}
\keyword{internal}
